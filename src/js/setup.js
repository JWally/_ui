/*
 * This basically exists as a cheat object
 * that holds static content that we want access
 * to later...
 *
 *
 */

define([], function () {
    var Setup = {
        baseUrl: "http://cpucnd1277xr9.compassbnk.com:3003/"
    };
    return Setup;
});
